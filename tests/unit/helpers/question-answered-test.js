
import { questionAnswered } from 'js-message-board/helpers/question-answered';
import { module, test } from 'qunit';

module('Unit | Helper | question answered');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionAnswered([42]);
  assert.ok(result);
});

